{
  "name": "casalpoglio-image-lambdas",
  "version": "1.0.0",
  "private": true,
  "engines": { "node": ">=22.17.0" },
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check goal-image/index.js && node --check lineup-image/index.js && node --check final-result-image/index.js"
  },
  "dependencies": {
    "@sparticuz/chromium": "149.0.0",
    "puppeteer-core": "25.1.0"
  }
}
