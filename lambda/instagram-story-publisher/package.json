{
  "name": "casalpoglio-instagram-story-publisher",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22.17.0"
  },
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check index.js && node --check image.js && node --check meta.js && node --check security.js && node --check storage.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.883.0",
    "@aws-sdk/client-ssm": "^3.883.0",
    "@aws-sdk/s3-request-presigner": "^3.883.0",
    "sharp": "^0.35.4"
  }
}
